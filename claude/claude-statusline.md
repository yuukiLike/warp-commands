# Claude statusline

Enter this inside a Claude Code conversation:

```text
/statusline show model name, context usage bar, cost, and git branch
```

This is a Claude conversation command, not a shell workflow.
